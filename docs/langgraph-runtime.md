# LangGraph Runtime

LangGraph is the orchestration kernel for graph execution, human-in-the-loop pauses, durable checkpoints, and resumable workflows. Oracle AI Database 26ai is the durable checkpoint and memory store. Valkey 8.1 is only a transient cache.
